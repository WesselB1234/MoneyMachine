package MoneyMachine.models.enums;

public enum ErrorType {
    BAD_REQUEST,
    UNAUTHORIZED,
    NOT_FOUND,
    INTERNAL_SERVER_ERROR,
    FORBIDDEN,
    UNPROCESSABLE_ENTITY,
    INVALID_AUTH_TOKEN
}